import { Default } from 'components/layouts/Default';
import { Gas } from 'components/templates/gas';

const GasPage = () => {
  return (
    <Default pageName="Gas">
      <Gas />
    </Default>
  );
};

export default GasPage;